#ifndef _USER_CONFIG_OVERRIDE_H_
#define _USER_CONFIG_OVERRIDE_H_

/******************************************************************
 * 【关键】配置版本号，每次修改这里 +1
 * 作用：强制设备重新加载新配置，避免旧配置不更新
 * 数值随便填，只要和之前不一样即可
 *****************************************************************/
#define CFG_HOLDER 202604051

// 永久开启串口命令输入，解决串口无响应问题
#define USE_SERIAL_BRIDGE
#define SERIAL_BAUDRATE 115200

/******************************************************************
 * 自定义GPIO模板（你提供的官方模板）
 * 作用：烧录后自动加载引脚功能，不需要WiFi、不需要网页、不需要串口
 * 格式：JSON 固定结构
 * NAME    = 设备名称
 * GPIO[]  = 每个GPIO对应的功能编号
 * FLAG    = 模板标志（默认0）
 * BASE    = 基础模块（1=通用ESP32）
 *****************************************************************/
#define USER_TEMPLATE "{\"NAME\":\"ESP32C3\",\"GPIO\":[0,0,0,0,0,576,0,0,0,32,0,0,0,0,0,0,0,0,1056,1088,0,0],\"FLAG\":0,\"BASE\":1}"

/******************************************************************
 * 模块类型设置
 * 0 = 使用自定义模板（USER_TEMPLATE）
 * 1~xxx = 使用官方预设模块
 *****************************************************************/
#define MODULE 0

/******************************************************************
 * 【可选】永久开启串口命令输入
 * 解决：串口能打印但发命令没反应
 * 不需要可以删除
 *****************************************************************/
#define SERIAL_CONSOLE_INPUT 1

#endif
